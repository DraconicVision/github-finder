import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    {/* The component rendered here is called the 'Main App Component.' */}
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
